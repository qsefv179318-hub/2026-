package com.livetaxlow.taxfeedback.transactionsync;

// Internal category used by the sync pipeline after analyzing store type text.
public enum InternalTransactionCategory {
    MEDICAL,
    FOOD,
    TRANSPORT,
    GENERAL
}
